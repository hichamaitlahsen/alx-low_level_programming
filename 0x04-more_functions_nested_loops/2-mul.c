#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies two integers
 * @x: first the number to multiply
 * @y: Second number multiple with first number
 * Return: the result of multiplication  between x parameter and y parameter
 */
int mul(int x, int y)
{
	return (x * y);
}
